{
  "name": "web",
  "version": "1.0.0",
  "description": "$ json-server -w db.json",
  "main": "index.js",
  "scripts": {
    "start:db": "json-server -w -p 1235 db.json", 
    "start:parcel": "parcel index.html" 
  },
  "dependencies": {
    "axios": "^0.19.2"
  }
}
